version: 2

models:

    - name: dim_categories
      description: Mart dimension table without foreign keys, having category id and attributes and basic aggregations. 

    - name: dim_venues
      description: Mart dimension table without foreign keys, having venue id and attributes and basic aggregations.

    - name: dim_dates
      description: Mart dimension table without foreign keys, having date id and attributes.

    - name: dim_users
      description: Mart dimension table without foreign keys, having user id and attributes.

    - name: dim_events
      description: Mart dimension table without foreign keys, having event id, attributes and basic aggregations.

    
